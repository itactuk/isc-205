#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y;
    printf("%cDigita 2 #s: ", 130);
    scanf("%d", &x);
    scanf("%d", &y);
    printf("La suma es: %d\n", x + y);
    return 0;
}
